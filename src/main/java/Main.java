import javax.swing.*;
import java.awt.*;
import java.io.IOException;
import java.util.HashMap;

public class Main {

    public static void main(String [] arggs) throws IOException {

        Rectangle bounds=new Rectangle(50,50,600,600);// create object with type Rectangle , where we keep bounds
        LoginPage loginPage=new LoginPage(bounds);
        //TextEditor editor=new TextEditor(bounds);

    }
}
